name: Verify s GUI and Record Result

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/verify-s-record.yml'
      - 'os/real-multiboot/chunk-compat.js'
      - 'os/real-multiboot/index.html'
      - 'os/real-multiboot/assets/iso-meta.json'

permissions:
  contents: write

concurrency:
  group: verify-s-gui-record
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install browser
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Boot deployed GUI
        env:
          TARGET_URL: https://gorics.github.io/website/os/real-multiboot/?s-record=${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          mkdir -p /tmp/s-proof
          cat > /tmp/verify-s-record.mjs <<'JS'
          import { chromium } from 'playwright';
          import fs from 'node:fs';

          const file = '/tmp/s-proof/result.json';
          const result = {
            success: false,
            state: 'starting',
            version: 's',
            run_id: process.env.GITHUB_RUN_ID,
            target: process.env.TARGET_URL,
            started_at: new Date().toISOString(),
            console: [],
            failed_requests: [],
          };
          const save = (state, extra = {}) => {
            Object.assign(result, extra, { state, updated_at: new Date().toISOString() });
            fs.writeFileSync(file, JSON.stringify(result, null, 2) + '\n');
          };
          const fatal = /I\/O error, dev sr0|SQUASHFS error|unable to find a medium containing a live file system/i;
          save('starting');

          let browser;
          let page;
          try {
            browser = await chromium.launch({
              headless: true,
              args: ['--no-sandbox', '--disable-dev-shm-usage', '--disable-background-timer-throttling', '--disable-renderer-backgrounding'],
            });
            const context = await browser.newContext({ viewport: { width: 1280, height: 900 } });
            page = await context.newPage();
            page.setDefaultTimeout(120000);
            page.on('console', message => {
              result.console.push(`${message.type()}: ${message.text()}`);
              if (result.console.length > 600) result.console.shift();
            });
            page.on('requestfailed', request => result.failed_requests.push({
              url: request.url(),
              error: request.failure()?.errorText || 'failed',
            }));

            const response = await page.goto(process.env.TARGET_URL, { waitUntil: 'domcontentloaded', timeout: 120000 });
            save('page-opened', { page_http: response?.status() || null });
            await page.waitForFunction(() => {
              const log = document.querySelector('#log')?.textContent || '';
              return log.includes('ISO page. s') && log.includes('v86 ISO chunk compatibility bridge installed');
            }, null, { timeout: 300000 });
            await page.click('#boot-btn');
            save('boot-clicked');

            await page.waitForFunction(() => {
              const log = document.querySelector('#log')?.textContent || '';
              if (/I\/O error, dev sr0|SQUASHFS error|unable to find a medium containing a live file system/i.test(log)) {
                throw new Error('fatal live-media error');
              }
              const canvas = document.querySelector('#screen canvas');
              const text = document.querySelector('#screen > div');
              return log.includes('v86 chunk URL rewrite')
                && canvas
                && canvas.width >= 640
                && canvas.height >= 480
                && getComputedStyle(canvas).display !== 'none'
                && (!text || getComputedStyle(text).display === 'none')
                && document.querySelector('#screen')?.classList.contains('active');
            }, null, { timeout: 1200000 });

            await page.waitForTimeout(30000);
            const details = await page.evaluate(() => {
              const canvas = document.querySelector('#screen canvas');
              const text = document.querySelector('#screen > div');
              const log = document.querySelector('#log')?.textContent || '';
              return {
                canvas_width: canvas?.width || 0,
                canvas_height: canvas?.height || 0,
                canvas_display: canvas ? getComputedStyle(canvas).display : 'missing',
                text_display: text ? getComputedStyle(text).display : 'missing',
                screen_active: document.querySelector('#screen')?.classList.contains('active') || false,
                boot_button_text: document.querySelector('#boot-btn')?.textContent || '',
                rewrite_observed: log.includes('v86 chunk URL rewrite'),
                sr0_error_observed: /I\/O error, dev sr0/i.test(log),
                squashfs_error_observed: /SQUASHFS error/i.test(log),
                log_tail: log.split('\n').slice(-500),
              };
            });
            if (fatal.test(details.log_tail.join('\n'))) throw new Error('fatal ISO read error after GUI appeared');
            await page.screenshot({ path: '/tmp/s-proof/gui.png', fullPage: true });
            const success = details.canvas_width >= 640
              && details.canvas_height >= 480
              && details.canvas_display !== 'none'
              && details.text_display === 'none'
              && details.screen_active
              && details.boot_button_text === 'Running'
              && details.rewrite_observed
              && !details.sr0_error_observed
              && !details.squashfs_error_observed;
            save('completed', { ...details, success, completed_at: new Date().toISOString() });
            if (!success) throw new Error('GUI verification assertions failed');
          } catch (error) {
            let details = {};
            try {
              details = await page?.evaluate(() => {
                const log = document.querySelector('#log')?.textContent || '';
                const canvas = document.querySelector('#screen canvas');
                return {
                  canvas_width: canvas?.width || 0,
                  canvas_height: canvas?.height || 0,
                  log_tail: log.split('\n').slice(-600),
                };
              }) || {};
              await page?.screenshot({ path: '/tmp/s-proof/failure.png', fullPage: true });
            } catch {}
            save('failed', { ...details, success: false, error: String(error?.stack || error), completed_at: new Date().toISOString() });
            throw error;
          } finally {
            await browser?.close().catch(() => {});
          }
          JS
          node --check /tmp/verify-s-record.mjs
          node /tmp/verify-s-record.mjs

      - name: Upload proof
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: s-gui-record-proof-${{ github.run_id }}
          path: /tmp/s-proof/
          if-no-files-found: warn

      - name: Record verification result
        if: always()
        env:
          JOB_STATUS: ${{ job.status }}
        run: |
          set -euo pipefail
          if test ! -s /tmp/s-proof/result.json; then
            mkdir -p /tmp/s-proof
            printf '{"success":false,"state":"workflow-failed-before-browser","version":"s","run_id":"%s","job_status":"%s"}\n' "$GITHUB_RUN_ID" "$JOB_STATUS" > /tmp/s-proof/result.json
          fi
          python3 - <<'PY'
          import json, os
          from pathlib import Path
          path = Path('/tmp/s-proof/result.json')
          data = json.loads(path.read_text())
          data['job_status'] = os.environ.get('JOB_STATUS')
          data['workflow_sha'] = os.environ.get('GITHUB_SHA')
          path.write_text(json.dumps(data, indent=2) + '\n')
          PY
          cp /tmp/s-proof/result.json /tmp/s-result.json
          git config user.name github-actions
          git config user.email github-actions@github.com
          for attempt in $(seq 1 10); do
            git fetch origin main
            git checkout -B main origin/main
            mkdir -p os/iso
            cp /tmp/s-result.json os/iso/s-chunk-bridge-result.json
            git add os/iso/s-chunk-bridge-result.json
            git commit -m 'Record s chunk bridge GUI verification [skip ci]' || exit 0
            if git push origin HEAD:main; then
              exit 0
            fi
            sleep $((attempt * 3))
          done
          exit 1
