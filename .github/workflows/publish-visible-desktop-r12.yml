name: Publish Visible Desktop R12 Superseded

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Use publish-visible-desktop-r12-v2.yml.'
