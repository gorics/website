name: Capture Public q Browser Failure

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/capture-legacy-publish-failure.yml'

permissions:
  contents: write
  actions: read

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Capture failed browser log
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_ID: '28912917591'
        run: |
          mkdir -p os/iso
          gh run view "$RUN_ID" --log-failed > /tmp/failed.log 2>&1 || true
          tail -n 1000 /tmp/failed.log > os/iso/public-q-browser-failure.log
          cat os/iso/public-q-browser-failure.log
          git config user.name github-actions
          git config user.email github-actions@github.com
          git fetch origin main
          git checkout -B main origin/main
          git add os/iso/public-q-browser-failure.log
          git commit -m 'Record public q browser verification failure [skip ci]' || true
          git push origin HEAD:main
