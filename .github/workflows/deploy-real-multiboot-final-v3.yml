name: Deploy And Verify Real Multiboot Final V3

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read
  actions: read
  pages: write
  id-token: write

concurrency:
  group: final-v3-pages
  cancel-in-progress: true

jobs:
  deploy-and-verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    environment:
      name: github-pages
    env:
      DEPLOY_VERSION: ${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
      PAGES_ARTIFACT: github-pages-${{ github.run_id }}-${{ github.run_attempt }}
      PAGE_ROOT: https://gorics.github.io/website
    steps:
      - name: Checkout immutable source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Wait for branch Pages propagation
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          seen=0
          for attempt in $(seq 1 90); do
            gh run list --repo "$GITHUB_REPOSITORY" --commit "$GITHUB_SHA" --limit 100 \
              --json workflowName,status,databaseId,event > /tmp/current-sha-runs.json
            count=$(jq '[.[] | select(.workflowName == "pages-build-deployment")] | length' /tmp/current-sha-runs.json)
            pending=$(jq '[.[] | select(.workflowName == "pages-build-deployment" and .status != "completed")] | length' /tmp/current-sha-runs.json)
            if [ "$count" -gt 0 ]; then seen=1; fi
            echo "branch-pages attempt=$attempt seen=$seen count=$count pending=$pending"
            if [ "$seen" = 1 ] && [ "$pending" = 0 ]; then break; fi
            sleep 5
          done
          sleep 90

      - name: Build R17 predictive site
        run: |
          set -euo pipefail
          bash scripts/build-real-multiboot-pages-final.sh _site 2>&1 | tee /tmp/final-v3-build.log
          node --check _site/os/real-multiboot/performance-mode.js
          node --check _site/os/real-multiboot/predictive-prefetch.js
          node --check _site/os/real-multiboot/safe-diagnostics.js
          grep -q 'r17-predictive' _site/os/real-multiboot/index.html
          grep -q 'predictive-prefetch.js?v=' _site/os/real-multiboot/index.html
          grep -q 'cancelBackgroundWork' _site/os/real-multiboot/predictive-prefetch.js
          grep -q 'constructorPoll' _site/os/real-multiboot/performance-mode.js
          grep -q '__GORICS_LAST_VM_OPTIONS__' _site/os/real-multiboot/performance-mode.js

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload unique Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          name: ${{ env.PAGES_ARTIFACT }}
          path: _site

      - name: Deploy R17 artifact attempt 1
        id: deploy1
        continue-on-error: true
        uses: actions/deploy-pages@v4
        with:
          artifact_name: ${{ env.PAGES_ARTIFACT }}
          timeout: 600000
          error_count: 20

      - name: Cool down after deployment conflict
        if: steps.deploy1.outcome == 'failure'
        run: sleep 45

      - name: Deploy R17 artifact attempt 2
        id: deploy2
        if: steps.deploy1.outcome == 'failure'
        uses: actions/deploy-pages@v4
        with:
          artifact_name: ${{ env.PAGES_ARTIFACT }}
          timeout: 600000
          error_count: 30

      - name: Confirm initial deployment
        run: |
          set -euo pipefail
          echo "deploy1=${{ steps.deploy1.outcome }} deploy2=${{ steps.deploy2.outcome }}"
          if [ "${{ steps.deploy1.outcome }}" != success ] && [ "${{ steps.deploy2.outcome }}" != success ]; then
            exit 1
          fi

      - name: Verify exact public R17 assets
        run: |
          set -euo pipefail
          PAGE="$PAGE_ROOT/os/real-multiboot"
          ready=0
          for attempt in $(seq 1 90); do
            stamp="$GITHUB_RUN_ID-$attempt-$(date +%s)"
            if curl -fsSL --max-time 60 -H 'Cache-Control: no-cache' "$PAGE/?verify=$stamp" -o /tmp/final-v3-index.html \
              && grep -q "$DEPLOY_VERSION" /tmp/final-v3-index.html \
              && grep -q 'predictive-prefetch.js?v=' /tmp/final-v3-index.html \
              && curl -fsSL --max-time 60 "$PAGE/predictive-prefetch.js?v=$DEPLOY_VERSION" -o /tmp/final-v3-prefetch.js \
              && grep -q 'cancelBackgroundWork' /tmp/final-v3-prefetch.js \
              && curl -fsSL --max-time 60 "$PAGE/performance-mode.js?v=$DEPLOY_VERSION" -o /tmp/final-v3-performance.js \
              && grep -q 'constructorPoll' /tmp/final-v3-performance.js \
              && curl -fsSL --max-time 60 "$PAGE/app.js?v=$DEPLOY_VERSION" -o /tmp/final-v3-app.js \
              && grep -q '/website/vendor/v86/images/buildroot-bzimage68.bin' /tmp/final-v3-app.js \
              && ! grep -q 'https://i.copy.sh/' /tmp/final-v3-app.js \
              && curl -fsSL --max-time 60 "$PAGE_ROOT/vendor/v86/manifest.json?v=$stamp" -o /tmp/final-v3-manifest.json \
              && grep -q "$DEPLOY_VERSION" /tmp/final-v3-manifest.json; then
              ready=1
              break
            fi
            sleep 10
          done
          test "$ready" = 1

          for image in buildroot-bzimage68.bin linux4.iso linux.iso freedos722.img; do
            code=$(curl -sS -L --max-time 120 -r 0-4095 \
              -o "/tmp/final-v3-$image.probe" -w '%{http_code}' \
              "$PAGE_ROOT/vendor/v86/images/$image?v=$DEPLOY_VERSION")
            case "$code" in 200|206) ;; *) exit 1;; esac
            test -s "/tmp/final-v3-$image.probe"
          done

          curl -fsSL --max-time 60 "$PAGE/assets/iso-meta.json?v=$DEPLOY_VERSION" -o /tmp/final-v3-iso-meta.json
          curl -fsSL --max-time 90 "$PAGE/assets/vmlinuz?v=$DEPLOY_VERSION" -o /tmp/final-v3-vmlinuz
          curl -fsSL --max-time 120 "$PAGE/assets/initrd.img?v=$DEPLOY_VERSION" -o /tmp/final-v3-initrd.img
          python3 - <<'PY'
          import hashlib, json
          from pathlib import Path
          meta = json.loads(Path('/tmp/final-v3-iso-meta.json').read_text())
          assert meta['architecture'] == 'i386'
          assert meta['name'] == 'gorics-linux-gui-web-i386-r12.iso'
          for filename, key in (('final-v3-vmlinuz', 'kernel'), ('final-v3-initrd.img', 'initrd')):
              data = (Path('/tmp') / filename).read_bytes()
              expected = meta[key]
              assert len(data) == int(expected['size'])
              assert hashlib.sha256(data).hexdigest() == expected['sha256']
          PY

      - name: Install Chromium
        run: |
          npm init -y
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Verify mobile OS switch and Buildroot startup
        env:
          VERIFY_URL: https://gorics.github.io/website/os/real-multiboot/?verify=${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          node <<'JS'
          const fs = require('fs');
          const { chromium } = require('playwright');
          const report = { url: process.env.VERIFY_URL, pageErrors: [], failedRequests: [] };
          (async () => {
            let browser;
            let page;
            try {
              browser = await chromium.launch({ headless: true, args: ['--no-sandbox', '--disable-dev-shm-usage'] });
              page = await browser.newPage({ viewport: { width: 390, height: 844 }, isMobile: true, hasTouch: true });
              page.on('pageerror', error => report.pageErrors.push(error.message));
              page.on('requestfailed', request => report.failedRequests.push({ url: request.url(), failure: request.failure() }));
              await page.goto(process.env.VERIFY_URL, { waitUntil: 'domcontentloaded', timeout: 120000 });
              await page.waitForFunction(() => Boolean(window.__GORICS_PERFORMANCE_MODE__ && window.__GORICS_PREDICTIVE_PREFETCH__), null, { timeout: 30000 });
              await page.selectOption('#os-select', 'buildroot');
              await page.waitForTimeout(250);
              const startedAt = Date.now();
              await page.click('#boot-btn');
              await page.waitForFunction(() => Boolean(window.goricsRealMultiboot && window.__GORICS_LAST_VM_OPTIONS__), null, { timeout: 180000 });
              await page.waitForFunction(() => /emulator-started/.test(document.querySelector('#log')?.textContent || ''), null, { timeout: 180000 });
              report.result = await page.evaluate((startedAt) => ({
                elapsedMs: Date.now() - startedAt,
                options: window.__GORICS_LAST_VM_OPTIONS__,
                prefetch: window.__GORICS_PREDICTIVE_PREFETCH__.getStats(),
                log: (document.querySelector('#log')?.textContent || '').slice(-16000),
              }), startedAt);
              const MB = 1024 * 1024;
              if (report.result.options.memory_size > 112 * MB) throw new Error('Buildroot RAM cap failed');
              if (report.result.options.vga_memory_size > 4 * MB) throw new Error('Buildroot VGA cap failed');
              if (report.result.prefetch.lastPreset !== 'buildroot') throw new Error('prefetch switch failed');
              if (/RangeError: Length out of range of buffer/i.test(report.result.log)) throw new Error('RangeError returned');
              if (report.pageErrors.length) throw new Error(report.pageErrors.join('\n'));
              report.success = true;
            } catch (error) {
              report.success = false;
              report.error = { message: error.message, stack: error.stack };
              if (page) {
                report.failureState = await page.evaluate(() => ({
                  prefetch: window.__GORICS_PREDICTIVE_PREFETCH__?.getStats?.() || null,
                  options: window.__GORICS_LAST_VM_OPTIONS__ || null,
                  log: (document.querySelector('#log')?.textContent || '').slice(-20000),
                })).catch(() => null);
              }
            } finally {
              fs.writeFileSync('/tmp/final-v3-performance-result.json', JSON.stringify(report, null, 2));
              if (page) await page.screenshot({ path: '/tmp/final-v3-performance-mobile.png', fullPage: true }).catch(() => {});
              if (browser) await browser.close();
            }
            if (!report.success) process.exit(1);
          })();
          JS

      - name: Probe for late branch overwrite
        id: late1
        run: |
          sleep 45
          curl -fsSL --max-time 60 -H 'Cache-Control: no-cache' \
            "$PAGE_ROOT/os/real-multiboot/?late1=$GITHUB_RUN_ID-$(date +%s)" \
            -o /tmp/final-v3-late1-index.html
          if grep -q "$DEPLOY_VERSION" /tmp/final-v3-late1-index.html; then
            echo 'reassert=false' >> "$GITHUB_OUTPUT"
          else
            echo 'reassert=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Reassert R17 artifact after late overwrite
        if: steps.late1.outputs.reassert == 'true'
        uses: actions/deploy-pages@v4
        with:
          artifact_name: ${{ env.PAGES_ARTIFACT }}
          timeout: 600000
          error_count: 30

      - name: Probe second late overwrite
        id: late2
        run: |
          sleep 45
          curl -fsSL --max-time 60 -H 'Cache-Control: no-cache' \
            "$PAGE_ROOT/os/real-multiboot/?late2=$GITHUB_RUN_ID-$(date +%s)" \
            -o /tmp/final-v3-late2-index.html
          if grep -q "$DEPLOY_VERSION" /tmp/final-v3-late2-index.html; then
            echo 'reassert=false' >> "$GITHUB_OUTPUT"
          else
            echo 'reassert=true' >> "$GITHUB_OUTPUT"
          fi

      - name: Reassert R17 artifact a second time
        if: steps.late2.outputs.reassert == 'true'
        uses: actions/deploy-pages@v4
        with:
          artifact_name: ${{ env.PAGES_ARTIFACT }}
          timeout: 600000
          error_count: 30

      - name: Final authoritative deployment verification
        run: |
          set -euo pipefail
          sleep 30
          ready=0
          for attempt in $(seq 1 30); do
            curl -fsSL --max-time 60 -H 'Cache-Control: no-cache' \
              "$PAGE_ROOT/os/real-multiboot/?final=$GITHUB_RUN_ID-$attempt-$(date +%s)" \
              -o /tmp/final-v3-stable-index.html
            if grep -q "$DEPLOY_VERSION" /tmp/final-v3-stable-index.html \
              && grep -q 'predictive-prefetch.js?v=' /tmp/final-v3-stable-index.html; then
              ready=1
              break
            fi
            sleep 10
          done
          test "$ready" = 1

      - name: Upload V3 R17 proof
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: final-real-multiboot-r17-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            /tmp/final-v3-build.log
            /tmp/current-sha-runs.json
            /tmp/final-v3-index.html
            /tmp/final-v3-late1-index.html
            /tmp/final-v3-late2-index.html
            /tmp/final-v3-stable-index.html
            /tmp/final-v3-app.js
            /tmp/final-v3-performance.js
            /tmp/final-v3-prefetch.js
            /tmp/final-v3-manifest.json
            /tmp/final-v3-iso-meta.json
            /tmp/final-v3-performance-result.json
            /tmp/final-v3-performance-mobile.png
          if-no-files-found: warn
